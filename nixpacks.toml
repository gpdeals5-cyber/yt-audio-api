providers = ["python"]

[phases.setup]
nixPkgs = ["ffmpeg", "python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn main:app --bind 0.0.0.0:8080 --workers 2 --threads 4 --timeout 300"
